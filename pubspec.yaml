name: softd
description: A new Flutter project.

publish_to: 'none' # Remove if you plan to publish

version: 1.0.0+1

environment:
  sdk: '^3.8.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  fluttertoast: ^8.2.4
dev_dependencies:                           
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0
  
flutter:
  uses-material-design: true

  assets:
    - assets/UC_Official_Seal.png
